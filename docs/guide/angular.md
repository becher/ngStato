# Angular

## Setup

```ts
import { provideStato } from '@ngstato/angular'

provideStato({
  devtools: true
})
```

## Store injection

Use `StatoStore` / `injectStore` for Angular integration with Signals.

